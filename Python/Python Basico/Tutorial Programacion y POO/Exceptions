#condicionales de error
#se ejecutan cuando sucede un error o puede suceder

#try, se coloca cuando puede surgir un error

#except, se coloca el error en si y se ejecuta su codigo, si el error sucede en el try, se ejecuta el try y la excepcion
#else, misma funcionalidad, pero en este caso, si no hay errores
#finally, codigo que siempre se ejecuta
#raise, permite crear errores

print("ejeccion")
y = int(input("coloque un numero"))
x = 3
print(y)
assert y != 0, "no esta definida la division en ese valor" #si no se cumple la condicion genera un error"


"""try: #permite que corra el codigo
    x = x/y
    print(x)

except ZeroDivisionError as x:
    print("esta dividiendo por 0")

finally:
    print("division realizada")"""
    
#el raise tiene la misma estrucutra de except