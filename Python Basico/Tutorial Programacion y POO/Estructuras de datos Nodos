#Nodos
#Linked structures. son nodos interconectados, para buscar uno recorre la lista hasta que llega al deseado
#depende propiamente de la estructura nodal, puede tener cola y cabeza o no tenerla

#crea la clase y solo usa dos parametros, un valor a guardar y a quien conduce
class Node(object):
    "Represents a single linked node."
    def __init__(self, data, next=None):
        self.data = data
        self.next = next


node1 = None
node2 = Node("A", None)
node3 = Node("B", node2)

#print(node1.data) #como no tiene nada, no se crea nada XD
print(node2.next)
print(node2.data)
print(node3.next)#muestra la representacion en memoria del nodo enlazado
print(node3.next.data)#muestra el dato del valor enlazado

#se pueden conectar dentro de una lista, o dejar esparcidos de acuerdo a la complejidad

#Todo depende de lso metoddos que se agreguen, eso le dara una funcionalidad

#Tambien se puede ver como extraer y añadir datos

#los stack se rigen , primero en entrar, ultimo en salir        ultimo en entrar, primero en salir
#Los Queues se rigen como una fila normal, primero en entrar, primero en salir.

#Se pone un metodo de almacenamiento, sea un array o una lista, se guarda, con uno se ejecuta un metodo para sacar lo ultimo, en el otro toca por indice